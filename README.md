Step 1 — Install date-fns
The package.json doesn't include date-fns yet. In your terminal (inside the color-clock folder),run 

npm install date-fns

Step 2 — Rewrite App.jsx
Open src/App.jsx, delete everything, and replace it.

Step 3 — Replace App.css
Open src/App.css, delete everything, and replace with this styled version:

Step 4 — Run It
In your terminal following this command 

npm run dev